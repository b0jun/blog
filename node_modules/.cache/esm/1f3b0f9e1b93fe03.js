let moongoose;_1b9‍.x([["default",()=>_1b9‍.o]]);_1b9‍.w("mongoose",[["default",["moongoose"],function(v){moongoose=v}]]);

//1.스키마 생성
const { Schema } = moongoose;

const PostSchema = new Schema({
  title: String,
  body: String,
  tags: [String], //문자열로 이루어진 배열
  publishedDate: {
    type: Date,
    default: Date.now //현재 날짜를 기본값으로 지정
  }
});

//2.모델 생성
const Post = moongoose.model("Post", PostSchema);
_1b9‍.d(Post);
